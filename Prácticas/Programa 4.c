#include <stdio.h>

/* Superficie del Triángulo.
El rpograma, al recibir como datos la base
y la altura de un tria'ngulo, calcula su
superficie.

BAS, ALT Y SUP: Variables de Tipo Real. */

void main (void)

{
    float BAS, ALT, SUP;
    printf ("Ingrese la Base y la Altura del Triángulo: ");
    scanf ("%f %f", &BAS, %ALT);
    SUP = BAS * ALT / 2;
    printf ("\nLa Superficie del Triángulo es: %5.2f", SUP);
    }
