#include <stdio.h>

/* Aplicación de Operadores. */

void main (void)

{
    int i, j, k = 2, 1 = 7;

    i = 9 + 3 * 2;
    j = 8 % 6 + * 2;
    i % = j;
    printf ("\nEl Valor de I es: %d", i);

    ++l;
    --k - = 1++ * 2;
    printf ("\nEl Valor de K es: %d", k);

    i = 5.5 - 3 * 2 % 4;
    j = (i * 2 - (k = 3, --k));
    printf ("\nEl Valor de J es: %d", j);
}
